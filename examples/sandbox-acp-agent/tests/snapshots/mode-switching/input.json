{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Use the bash tool to run exactly: cat notes.txt. Then reply with the single word DONE and stop." },
    { "op": "setConfigOption", "configId": "sandbox-mode", "value": "workspace-write" },
    { "op": "prompt", "text": "Use the bash tool to run exactly this one command in a single call: printf 'switched\\n' > out.txt && cat out.txt. Then reply with the single word DONE and stop." },
    { "op": "setConfigOption", "configId": "approval-policy", "value": "never" },
    { "op": "prompt", "text": "Without using any tools, state your current approval policy in one short sentence and stop." }
  ]
}
